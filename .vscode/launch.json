{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run acorn",
            "runtimeExecutable": "acorn",
            "cwd": "${workspaceFolder}",
            "args": []
        }
    ]
}